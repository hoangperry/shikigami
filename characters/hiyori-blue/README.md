# Hiyori (Live2D sample) · blue

Recolor variant of `hiyori` (preset: **blue**) generated by
`scripts/reskin-character.sh`. Same Live2D rig + motions; textures
recoloured via ImageMagick `-modulate` (preserves UV alignment).

Regenerate:
```bash
./scripts/reskin-character.sh hiyori hiyori-blue blue
```
